#lang setup/infotab
(define name "sshscript")
(define blurb
  '("Bindings to the ssh library"))
(define primary-file "ssh-ffi.rkt")
(define categories '(net))
(define scribblings '(("ssh.scrbl" ())))
